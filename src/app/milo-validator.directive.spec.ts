import { MiloValidatorDirective } from './milo-validator.directive';

describe('MiloValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MiloValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
